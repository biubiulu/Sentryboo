# 2026-09-11

- 完成 CodexMeter 全目录与 UI 配置梳理，并写入仓库根目录 `总结.md`。
- 要点：UI 由 MenuBarExtra + 独立 Window 场景驱动；配置集中在 `AppSettings`（UserDefaults），菜单栏精细外观为 `MenuBarAppearance`，弹层区块为 `PopoverContentConfiguration`；语言走 `Localizable.xcstrings`；历史窗口 macOS 26 Liquid Glass / 旧系统 Material 回退。

- 定稿二开设计：`docs/plans/2026-09-11-zabbix-menubar-alert-design.md`（15s 轮询 problem.get、AlertSource、一期 Problem 列表）。
- 清空原 CodexMeter 工程与无关文件；仓库仅保留 `总结.md`、`docs/plans/`、`.gitignore`、`.git`、`.codebuddy/`。

- 启动实现：应用名 Sentryboo；写入 `docs/plans/2026-09-11-sentryboo-m0-implementation.md`；创建 `Sentryboo.xcodeproj` + 源码骨架（MenuBarExtra、Settings、AlertSession、ZabbixSource、Keychain）；`xcodebuild` Debug **BUILD SUCCEEDED**。

- 修复设置窗「完全没反应」：`LSUIElement`/`MenuBarExtra` 下需先 `NSApp.activate`，再 `openWindow`，并对已有窗 `makeKeyAndOrderFront`；设置窗 identifier=`settings`。

- 完成 M3–M4：最低严重级别过滤、自签证书开关（`InsecureTLSDelegate`）、证书/空/错态文案、打开 Zabbix Web 与问题行跳转；更新 README（已知限制 + 手工验收清单）；`xcodebuild` Debug **BUILD SUCCEEDED**。一期实现计划任务已全部落地，待真机连 Zabbix 验收。

- 按用户选择将一期改动提交到 `main`：`8df00fd feat: replace CodexMeter with Sentryboo Zabbix menubar alerts`（71 files，+1989/-19585）；未 push；`.codebuddy/` 未纳入提交。
