# 2026-09-11

- 完成 CodexMeter 全目录与 UI 配置梳理，并写入仓库根目录 `总结.md`。
- 要点：UI 由 MenuBarExtra + 独立 Window 场景驱动；配置集中在 `AppSettings`（UserDefaults），菜单栏精细外观为 `MenuBarAppearance`，弹层区块为 `PopoverContentConfiguration`；语言走 `Localizable.xcstrings`；历史窗口 macOS 26 Liquid Glass / 旧系统 Material 回退。

- 定稿二开设计：`docs/plans/2026-09-11-zabbix-menubar-alert-design.md`（15s 轮询 problem.get、AlertSource、一期 Problem 列表）。
- 清空原 CodexMeter 工程与无关文件；仓库仅保留 `总结.md`、`docs/plans/`、`.gitignore`、`.git`、`.codebuddy/`。

- 启动实现：应用名 Sentryboo；写入 `docs/plans/2026-09-11-sentryboo-m0-implementation.md`；创建 `Sentryboo.xcodeproj` + 源码骨架（MenuBarExtra、Settings、AlertSession、ZabbixSource、Keychain）；`xcodebuild` Debug **BUILD SUCCEEDED**。

- 修复设置窗「完全没反应」：`LSUIElement`/`MenuBarExtra` 下需先 `NSApp.activate`，再 `openWindow`，并对已有窗 `makeKeyAndOrderFront`；设置窗 identifier=`settings`。

- 完成 M3–M4：最低严重级别过滤、自签证书开关（`InsecureTLSDelegate`）、证书/空/错态文案、打开 Zabbix Web 与问题行跳转；更新 README（已知限制 + 手工验收清单）；`xcodebuild` Debug **BUILD SUCCEEDED**。一期实现计划任务已全部落地，待真机连 Zabbix 验收。

- 按用户选择将一期改动提交到 `main`：`8df00fd feat: replace CodexMeter with Sentryboo Zabbix menubar alerts`（71 files，+1989/-19585）；未 push；`.codebuddy/` 未纳入提交。

- 真机反馈：测试连接成功（API 7.0.22）但同步失败 `not allowed ... Sorting by field "severity"`。根因是 `problem.get` 的 `sortfield` 仅允许 `eventid`。已改为服务端按 `eventid` DESC，客户端再按 severity/时间排序；设置页增加可开关 DEBUG 模式（脱敏请求/响应日志，可复制）；`xcodebuild` Debug **BUILD SUCCEEDED**。

- 真机反馈：告警列表混入很久以前的历史。根因是 `recent: true` 会带上近期已恢复问题。已改为 `recent: false`，并按 `r_eventid` 过滤，只显示当前未恢复告警；`xcodebuild` Debug **BUILD SUCCEEDED**。

- 用户确认环境为 Zabbix **5.4.1**，目标是「监视 → 问题 → 最近问题 → 灾难」。已改回对齐前端默认：`recent=true`、`suppressed=false`，并丢弃无主机/已删除主机问题；灾难级别靠设置「最低严重级别=灾难」。DEBUG 里那些 `r_eventid=0` 实际是 7–9 月仍未恢复的灾难问题，不是历史已恢复。`xcodebuild` Debug **BUILD SUCCEEDED**。

- 多 Zabbix 账号已落地：设计 `docs/plans/2026-09-11-multi-zabbix-accounts-design.md`；实现账号列表+编辑、Keychain 分槽 `zabbix.apiToken.<uuid>`、并行拉取合并、部分失败状态、旧单账号迁「默认」；`xcodebuild` Debug **BUILD SUCCEEDED**。

- Token 改存 Application Support 加密文件：设计 `docs/plans/2026-09-11-token-file-store-design.md`；新增 `TokenStore`（AES-GCM 整文件），`AlertSession` 改用其读写；启动从旧 Keychain 迁入后删除；删除 `KeychainStore.swift`；README/MEMORY 已更新；`xcodebuild` Debug **BUILD SUCCEEDED**。

- 可配置轮询间隔 + 静默刷新呼吸灯：设计 `docs/plans/2026-09-11-poll-interval-silent-refresh-design.md`；`zabbix.pollIntervalSeconds` 默认 15（5–300）；刷新不再切 `.syncing`，弹层「已连接 · 无问题」右侧绿色呼吸灯表示刚刷过；`xcodebuild` Debug **BUILD SUCCEEDED**。

- 菜单栏图标最终不用图片：改为状态栏彩色呼吸灯。绿呼吸=正常（含非灾难告警）；红呼吸=列表存在灾难级；静态红=未配置或连接失败。已删除 `MenuBarIcon.imageset`。
- 修复菜单栏圆点透明/变黑：`MenuBarExtra` 会把 SwiftUI Shape 当模板渲染；改为 `NSImage(isTemplate=false)` + `.renderingMode(.original)` 保留绿/红。
- 菜单栏点缩小到 8pt；呼吸改用 `TimelineView(.periodic)`（MenuBarExtra label 上 `repeatForever` 不稳定）。
- `linkd.autoShortcut` / Process Instance Registry 是 macOS 26 噪音，不是编译失败。真正“看不到 App”是因为 `LSUIElement` 不进 Dock；另加 `AppDelegate`：禁用 Automatic Termination、按 `NSStatusItem Visible` 在 accessory/regular 间切换并清隐藏标记。呼吸灯改 AppKit `Timer` 驱动（去掉 TimelineView）。
- MenuBarExtra label 在 macOS 26 会强制 template（透明/点一下变黑坨）。已弃用 MenuBarExtra，改 `NSStatusItem` + `StatusDotView` 自绘彩色呼吸灯 + `NSPopover` 弹层；设置走 `Settings` 场景/`AppDelegate.openSettingsWindow`。
