# CodexMeter 项目与 UI 配置梳理

> 扫描范围：`/Users/luhonglin/Desktop/java/Sentryboo`  
> 重点：目录结构、UI 场景、设置持久化、外观与弹层配置链路

---

## 1. 项目概览

**CodexMeter** 是原生 macOS **菜单栏应用**（SwiftUI + AppKit），用于在菜单栏实时显示 Codex 账号剩余配额，无需打开 Codex。

| 项 | 说明 |
|---|---|
| 当前版本 | 1.5.5（build 20） |
| 最低系统 | macOS 13+ |
| 产品形态 | 仅菜单栏（`LSUIElement=true`，无 Dock 图标） |
| 核心依赖 | Apple 框架为主；**Sparkle** 负责签名更新 |
| 业务可测层 | `Package.swift` → `CodexMeterCore`（`CodexMeter/Core`） |
| 数据来源 | 本地 `codex app-server`（stdio JSON-RPC），不刮网页、不读 token 文件 |

---

## 2. 目录结构

```
Sentryboo/
├── CodexMeter/                 # App 主体（SwiftUI / AppKit UI + 服务）
│   ├── CodexMeterApp.swift     # @main：MenuBarExtra + 独立 Window 场景
│   ├── ContentView.swift       # 菜单栏弹层主 UI / Settings / 配额行
│   ├── MenuBarProgressView.swift
│   ├── PopoverCustomizationView.swift
│   ├── DeveloperOptionsView.swift
│   ├── UsageHistoryView.swift
│   ├── HistoryChartComponents.swift
│   ├── AboutView.swift
│   ├── AppSettings.swift       # 用户偏好总入口（UserDefaults）
│   ├── L10n.swift + Localizable.xcstrings
│   ├── Assets.xcassets/        # AppIcon 等
│   └── Core/                   # 纯逻辑：配额、外观模型、历史、SQLite…
├── Config/CodexMeter-Info.plist
├── CodexMeter.xcodeproj/
├── Package.swift               # CodexMeterCore + 单测
├── Tests/CodexMeterCoreTests/
├── Scripts/                    # Sparkle / ad-hoc 签名脚本
├── docs/images/
├── README.md / AGENTS.md / LICENSE / appcast.xml
└── 总结.md                     # 本文件
```

### UI 相关核心文件职责

| 文件 | 职责 |
|---|---|
| `CodexMeterApp.swift` | 场景图：菜单栏 + 4 个独立 Window；外观同步 |
| `ContentView.swift` | 弹层布局、可配置区块、内嵌 Settings、配额/时间条 |
| `MenuBarProgressView.swift` | 菜单栏指示器（NSImage 绘制） |
| `AppSettings.swift` | 语言 / 外观 / 菜单栏样式 / 弹层内容 / 通知 / 开发者外观 |
| `Core/MenuBarAppearance.swift` | 菜单栏几何、颜色、字体等可编码外观值 |
| `Core/PopoverContentConfiguration.swift` | 弹层区块显隐、排序、菜单栏配额窗口选择 |
| `PopoverCustomizationView.swift` | 弹层内容定制独立窗口 |
| `DeveloperOptionsView.swift` | 开发者外观微调与预览 |
| `UsageHistoryView.swift` + `HistoryChartComponents.swift` | 历史窗口 + Charts + Liquid Glass |
| `AboutView.swift` | 关于 / 更新 / 隐私 / 本地数据 |
| `Localizable.xcstrings` | en / zh-Hans / zh-Hant 文案源 |

---

## 3. UI 场景架构（如何“挂”起来）

入口在 `CodexMeterApp`：

1. **`MenuBarExtra`（`.window` 样式）**  
   - **Label**：`MenuBarProgressView`（菜单栏图标/进度）  
   - **Content**：`ContentView`（点击后的弹层）

2. **独立 `Window` 场景**（避免 sheet 挂在 MenuBarExtra 上失焦即消失）

| Window ID | 视图 | 默认尺寸 | 备注 |
|---|---|---|---|
| `developer-options` | `DeveloperOptionsView` | 540×680 | contentSize |
| `popover-customization` | `PopoverCustomizationView` | 520×620 | contentSize |
| `usage-history` | `UsageHistoryView` | 900×820 | hiddenTitleBar，可缩放/全屏 |
| `about` | `AboutView` | 500×620 | contentSize |

打开方式：弹层内 `SettingsSection` / 历史入口通过 `@Environment(\.openWindow)` + `CodexMeterWindowID.*`。

### 外观同步（重要）

所有内容场景都套了 `.appAppearance(settings.appearanceMode)`：

- SwiftUI：`preferredColorScheme`
- AppKit：`WindowAppearanceSynchronizer` 写 `NSWindow.appearance`

原因：`MenuBarExtra` 的 material **不完全**听从 SwiftUI color scheme；必须同步宿主窗口。菜单栏 **Label 本身**仍跟随系统菜单栏外观。

---

## 4. UI 配置总链路

```
用户操作（Settings / 定制窗口 / 开发者选项）
        ↓
   AppSettings (@Published)
        ↓
   UserDefaults 持久化
        ↓
┌───────────────────┬────────────────────┬────────────────────────┐
│ menuBarStyle      │ developerAppearance│ popoverContent         │
│ + appearanceMode  │ (MenuBarAppearance)│ (PopoverContentConfig) │
└────────┬──────────┴─────────┬──────────┴───────────┬────────────┘
         ↓                    ↓                      ↓
 MenuBarProgressView    弹层 MetricBar 着色     ContentView 区块渲染
                        ResetCredits tint      菜单栏选哪个配额窗口
```

### 4.1 `AppSettings` 持久化 Key

| Key | 内容 | 默认 |
|---|---|---|
| `appearanceMode` | system / light / dark | system |
| `language` | system / en / zh-Hans / zh-Hant | system |
| `menuBarStyle` | 菜单栏展示样式 | `progressAndPercentage` |
| `popover.contentConfiguration` | JSON：弹层区块配置 | 见下节默认值 |
| `notificationsEnabled` | 通知开关 | false（按实现读取） |
| `notificationThreshold` | 10/20/30/40 | 20 |
| `history.retention` | 历史保留策略 | forever |
| `updates.includePrereleases` | 含预发布更新 | false |
| `developer.appearance` | JSON：菜单栏精细外观 | `acceptedV1` |
| `developer.appearanceDefaultsVersion` | 外观迁移版本 | 当前写 2 |
| `developer.preview*` | 开发者预览开关/预设/百分比 | 关 / normal / 72&55 |
| `history.identitySalt` | 账户历史分区盐 | 首次 UUID |

另外还有 **开机启动**（`ServiceManagement`），状态由 `launchAtLoginEnabled` 反映。

### 4.2 语言（L10n）

- 源：`Localizable.xcstrings`
- `AppSettings.language` 变更 → `L10n.setLanguage(code)`
- UI 一律走 `L10n.string` / `L10n.format`，不硬编码用户可见文案

---

## 5. 菜单栏指示器配置

### 5.1 样式：`MenuBarDisplayStyle`

| Case | 含义 |
|---|---|
| `progressAndPercentage` | 双环 + 百分比（默认/基线） |
| `horizontalBarBesidePercentage` | 横条在旁 |
| `compactBarBelowPercentage` | 紧凑条在百分比下方 |
| `dualBars` | 双条 |
| `percentageOnly` | 仅百分比 |
| `progressOnly` | 仅进度环 |

入口：弹层 Settings、`DeveloperOptionsView` 顶部 Picker。

### 5.2 精细外观：`MenuBarAppearance`

**基线（acceptedV1）要点：**

- 高度约 20pt，环直径 **18pt**
- 外环（配额）描边 **3pt**，内环（时间）**2pt**，默认时间色 **brightBlue**
- 可配：字号/字重/垂直偏移、caption、环间隙/起始角/轨道透明度、条宽高、padding、文字宽度
- 颜色档：normal / warning / critical / time / caption / stale
- 过期数据 `!` 指示：显隐、尺寸、leading/trailing

渲染：`MenuBarProgressView` 画成原色 `NSImage`；`attentionLevel` 决定用哪套状态色。  
数据来源：`popoverContent.selectedMenuBarWindow`（显式选择，或自动选剩余百分比最低的窗口）；开发者预览开启时用 fixture。

颜色解析注意：`MenuBarColorChoice.swiftUIColor(for:)` 会按当前 colorScheme 解析具体 NSColor，避免 AppKit Progress 在外观切换后丢 tint（弹层 Progress 还用 `.id(colorScheme)` 强制重建）。

---

## 6. 菜单栏弹层（ContentView）如何配置

### 6.1 布局结构

固定三层垂直兄弟，中间才滚动：

1. **Header**：刷新、账户状态等  
2. **中间可滚动区**：按配置渲染的动态内容（配额 / Reset Credits / 历史入口…）  
3. **底部固定**：Settings 折叠区 + 更新时间/Quit  

高度上限：`PopoverLayoutMetrics` 根据**当前弹层所在屏幕**的 `visibleFrame.height - 16` 计算；`PopoverScreenHeightReader` 跟踪换屏/分辨率变化。

### 6.2 可配置区块：`PopoverContentConfiguration`

区块枚举 `PopoverContentSection`：

| Section | UI |
|---|---|
| `quotaWindows` | `UsageWindowRow`（配额条 + 时间条 + pace + 重置倒计时） |
| `resetCredits` | `ResetCreditsSection`（有数据才显示） |
| `quotaHistory` | 紧凑周配额图 → 点开 History 窗口 |
| `tokenActivity` | 近 30 天 token 迷你柱图 → 点开 History 窗口 |

可配置项：

- `sectionOrder`：区块顺序  
- `hiddenSections`：整块隐藏（后台仍刷新/记历史）  
- `hiddenQuotaWindowIDs`：隐藏个别配额窗口  
- `menuBarQuotaWindowID`：菜单栏显示哪个窗口（`nil` = 自动最低剩余）

**默认：**

- 顺序 = 全部 section  
- 默认隐藏 GPT-Reserve 周桶（`base_model_inference` + 7 天 duration 生成的 historyID）  
- 菜单栏选择 = 自动  

定制 UI：独立窗口 `PopoverCustomizationView`（含 live preview）。

### 6.3 内嵌 Settings（`SettingsSection`）

折叠面板内直接改：

- 语言、应用外观、菜单栏样式  
- 通知开关 + 阈值阈值  
- 开机启动  
- 跳转：Popover 定制 / Developer Options / About  

---

## 7. 其他窗口 UI 要点

### 7.1 Usage History

- 独立可缩放窗口，透明集成标题栏 + 系统红绿灯（内容区不加 Close）
- macOS 26：`GlassEffectContainer` + `historyGlassCard()`（`glassEffect`）  
- 更早系统：`.ultraThinMaterial` 圆角卡片回退  
- 配额图与 token 图分离；自定义胶囊选择器；Charts 悬停规则见 `AGENTS.md`

### 7.2 Developer Options

- 实时预览菜单栏外观；滑条/颜色/过期指示器  
- 可复制 JSON、重置到 acceptedV1  
- 历史 fixture / 临时更新态等**不得污染**真实配额与发布状态  

### 7.3 About

- 版本/Build、GitHub、更新通道（含预发布）、自动安装偏好  
- 本地数据位置、隐私、许可证说明  

---

## 8. 资源与工程配置（UI 相关）

| 位置 | 作用 |
|---|---|
| `Assets.xcassets` | AppIcon 等；图标基线：暖象牙底 + 酒红抽象码 + 奶油分段计量条 |
| `Config/CodexMeter-Info.plist` | `LSUIElement`、Sparkle feed/公钥、最低系统版本占位 |
| `Package.swift` | Core 库平台 macOS 13，与 App 部署目标对齐 |

---

## 9. UI 配置「从哪改」速查

| 想改什么 | 去哪 |
|---|---|
| 菜单栏样式（环/条/百分比组合） | 弹层 Settings 或 Developer Options → `menuBarStyle` |
| 环粗细、颜色、字号、caption | Developer Options → `developerAppearance` |
| 弹层显示哪些块、顺序、哪个配额上菜单栏 | Popover Customization → `popoverContent` |
| 浅色/深色/跟随系统 | Settings → `appearanceMode` |
| 中英繁 | Settings → `language` + `Localizable.xcstrings` |
| 历史窗口玻璃/卡片 | `HistoryChartComponents.historyGlassCard` |
| 新独立面板 | `CodexMeterApp` 加 `Window` + `CodexMeterWindowID`，用 `openWindow` |

---

## 10. 设计约束（改 UI 时务必遵守）

1. **保持菜单栏-only**；需要复杂交互就用独立 `Window`，不要挂 sheet 到 MenuBarExtra。  
2. 弹层中间滚动、头/设置/退出固定，避免半透明控件下透出滚动内容。  
3. 隐藏的弹层区块仍要后台刷新与记历史。  
4. 菜单栏 Label 不嵌套动态布局容器；缺重置时间时省略时间指示。  
5. 配额与 token 指标分离；历史计算规则以 `AGENTS.md` 为准。  
6. 用户可见字符串走 String Catalog；源码注释用英文且只写非显然行为。  

---

## 11. 一句话总结

CodexMeter 的 UI 不是散落硬编码，而是 **「场景图（MenuBarExtra + 独立 Window）+ AppSettings 持久化 + 两份可编码配置（MenuBarAppearance / PopoverContentConfiguration）+ L10n」** 驱动：菜单栏绘制读外观与样式，弹层按区块配置组装，历史窗口在 macOS 26 用 Liquid Glass、旧系统用 Material 回退。
